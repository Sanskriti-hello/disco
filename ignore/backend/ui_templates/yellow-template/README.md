# Yellow Template

A bright yellow/gold themed React + TypeScript template with summary section, carousel boxes, and links sidebar.

Run locally:

```bash
cd yellow-template
npm install
npm start
```
