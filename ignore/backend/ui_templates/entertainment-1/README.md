# Entertainment Template

A vibrant red themed React + TypeScript template with large content boxes, carousel, and action buttons.

Run locally:

```bash
cd entertainment-template
npm install
npm start
```
